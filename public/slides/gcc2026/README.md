# GCC2026 slide assets

Drop demo placeholder assets here:

- `demo1-placeholder.svg` — MRSA history → notebook → graph extraction → workflow. Storyboard: `vault/projects/history_markdown/gcc2026/DEMO1_STORYBOARD.md`.
- `demo2-placeholder.svg` — Foundry interview → gxwf → Format 2. Storyboard: `vault/projects/history_markdown/gcc2026/DEMO2_STORYBOARD.md`.

When the videos are recorded, the MDX should be edited to swap the `<img>` for an `<iframe>` (YouTube embed) or `<video>` (local MP4 in this directory).
